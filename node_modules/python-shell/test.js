let {PythonShell} = require('./index')

//let pyshell = new PythonShell('test/python/exit-code.py')
//pyshell.on('close', ()=>{
//    console.log('closed')
//})
let options = {
    mode: 'text',
    pythonPath: 'C:/Users/eyasv/AppData/Local/Programs/Python/Python37-32/python.exe',
    pythonOptions: ['-u'], // get print results in real-time
    scriptPath: './test/python/',
    args: ["abs"]
};
PythonShell.run('echo_args.py', options, function (err, results) {
    if (err) throw err;
    // results is an array consisting of messages collected during execution
    console.log('results: %j', results);
});